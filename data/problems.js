/* The problem bank.
 *
 * DELIBERATELY SHUFFLED. Grouping by pattern would train execution while
 * destroying the thing being trained -- recognition. If you know it is a window
 * problem before you read it, you have skipped the only hard step.
 *
 * `pat` and `str` are the outbound links, and they are the ONLY declaration of
 * the relationship: pattern pages and structure pages build their problem lists
 * by inverting these at load time. Some problems have no clean pattern (pointer
 * mechanics, an XOR trick) and carry only a structure -- that is honest, and
 * better than forcing a label.
 *
 * Difficulty, pattern and insight stay hidden until solved or revealed.
 */
window.PROBLEMS = {
  howto: {
    title: 'How to use this list',
    steps: [
      'Pick the lowest unsolved number. Do not shop for a problem you like the look of — the shopping is itself avoidance.',
      'Before coding, write your reasoning on paper: the output type, n, the brute force, where it is redundant, and the pattern you are betting on.',
      'Give it 25 minutes. If you are stuck, reveal — but read only the pattern line and look away before the insight.',
      'Tick "solved" when YOU judge it solved. That reveals the difficulty, the pattern and the key insight, with links to both pages so you can read the deviations while it is fresh.',
      'Batch your reviews. Come back and say: "problem #23, here is my reasoning — what do you think?" Several at once is fine and is a better use of the conversation than solving one problem at a time in chat: you do the thinking, I only review it.',
      'Log every bug you make in one file as you go. That log will be worth more than the next twenty problems.',
    ],
    note: 'Sixty-one problems. Not small, but not 300 either — roughly three or four instances of each pattern, which is what recognition actually needs. Doing all of them well beats doing 200 badly.',
  },

  items: [
    { t: 'Two Sum', ask: 'Given an array and a target, return the indices of the two elements that add to the target.', d: 'Easy', pat: ['hash-count'], i: 'Store value → index as you scan and look for target − x. One pass, not two.' },
    { t: 'Merge intervals', ask: 'Given a list of intervals, merge all the overlapping ones and return the result.', d: 'Medium', pat: ['sweep'], i: 'Sort by start. Then the current interval either extends the last or begins a new one — only two cases.' },
    { t: 'Longest substring without repeating characters', ask: 'Find the length of the longest run in a string containing no repeated character.', d: 'Medium', pat: ['window'], i: 'Expand right always; while the incoming char is already inside, shrink from the left. Record while valid.' },
    { t: 'Validate BST', ask: 'Decide whether a binary tree satisfies the binary-search-tree property.', d: 'Medium', pat: ['tree-dfs'], i: 'Carry an allowed (low, high) range downward. Comparing each node only to its parent is the classic wrong answer.' },
    { t: 'Kth largest element', ask: 'Return the kth largest value in an unsorted array.', d: 'Medium', pat: ['heap-topk', 'quickselect'], i: 'A MIN-heap capped at k. O(n log k) rather than O(n log n). Quickselect gets O(n) average if pushed.' },
    { t: 'Number of islands', ask: 'Count the connected groups of land cells in a grid of land and water.', d: 'Medium', pat: ['graph-dfs'], str: ['grid'], i: 'A graph where the node is (row, col). Sink each island as you visit so it is never counted twice.' },
    { t: 'Climbing stairs', ask: 'Count the distinct ways to climb n stairs taking one or two steps at a time.', d: 'Easy', pat: ['memo'], tech: ['tabulation'], i: 'f(n) = f(n−1) + f(n−2). Recognising Fibonacci is the problem; then note only two variables are needed.' },
    { t: 'Daily temperatures', ask: 'For each day, how many days until a warmer temperature? Zero if none comes.', d: 'Medium', pat: ['deque-mono'], i: 'Stack of INDICES of days still waiting. When a warmer day arrives, pop and the answer is the index difference.' },
    { t: 'Course schedule', ask: 'Given courses and prerequisite pairs, decide whether all courses can be completed.', d: 'Medium', pat: ['topo'], tech: ['cycle-directed'], i: 'Kahn. Emit fewer nodes than there are courses and there is a cycle — the count IS the check.' },
    { t: 'Product of array except self', ask: 'For each position, return the product of every other element. No division.', d: 'Medium', pat: ['prefix'], i: 'A prefix pass and a suffix pass. When each position needs everything before and after, build both ends.' },
    { t: 'Meeting rooms II', ask: 'Given meeting intervals, find the minimum number of rooms required.', d: 'Medium', pat: ['sweep', 'heap-topk'], i: 'Two solutions worth knowing: +1/−1 events swept for the running maximum, or a heap holding the earliest free time.' },
    { t: 'Linked list cycle', ask: 'Detect whether a linked list contains a cycle, using constant extra space.', d: 'Easy', pat: ['fast-slow'], i: 'The gap closes by one per step inside a cycle, so they must meet. Null-check fast and fast.next.' },
    { t: 'Coin change', ask: 'Given coin denominations and an amount, return the fewest coins that make it, or −1.', d: 'Medium', pat: ['memo'], tech: ['knapsack'], i: 'dp[amount] = 1 + min over coins of dp[amount − coin]. Greedy is wrong, and knowing why is the point.' },
    { t: 'Group anagrams', ask: 'Group a list of words so that anagrams of each other end up together.', d: 'Medium', pat: ['hash-count'], i: 'The key is a canonical form — sorted letters, or a 26-length count tuple. Deriving a key is the generalisable move.' },
    { t: 'Binary tree level order traversal', ask: 'Return the node values level by level, top to bottom.', d: 'Medium', pat: ['tree-bfs'], i: 'Snapshot len(queue) BEFORE the inner loop. That count is exactly one level.' },
    { t: 'Koko eating bananas', ask: 'Given piles and h hours, find the minimum eating speed that finishes all piles in time.', d: 'Medium', pat: ['binary-answer'], i: 'Checking a speed is easy; constructing the answer is not. Say out loud why feasibility is monotonic in speed.' },
    { t: 'Subarray sum equals k', ask: 'Count the subarrays whose elements sum to exactly k.', d: 'Medium', pat: ['prefix', 'hash-count'], i: 'Map running prefix sum → times seen. Seed with {0: 1} for the empty prefix — the bug everyone hits.' },
    { t: 'Implement a trie', ask: 'Support insert, exact search, and startsWith over a set of words.', d: 'Medium', pat: ['trie'], i: 'Nested dicts plus an end-of-word flag. Without the flag, "car" matches when only "carpet" was inserted.' },
    { t: 'Move zeroes', ask: 'Move every zero to the end of the array in place, keeping the order of the others.', d: 'Easy', pat: ['two-pointers'], i: 'One read pointer, one write pointer. The write index is where the next non-zero belongs.' },
    { t: 'Rotting oranges', ask: 'Each minute, rot spreads to adjacent fresh oranges. How many minutes until none are fresh?', d: 'Medium', pat: ['tree-bfs'], str: ['grid'], i: 'Every rotten orange is in the queue before the first step. Then BFS levels are minutes directly.' },
    { t: 'Lowest common ancestor', ask: 'Find the deepest node that is an ancestor of both given nodes in a binary tree.', d: 'Medium', pat: ['tree-dfs'], i: 'Return a found node upward. The first node receiving a non-null result from both sides is the answer.' },
    { t: 'Top k frequent elements', ask: 'Return the k most frequent values in an array.', d: 'Medium', pat: ['heap-topk', 'hash-count'], tech: ['counting-sort'], i: 'Count, then a size-k heap. Bucketing by frequency gives O(n) if pressed — counts are bounded by n.' },
    { t: 'Search in a rotated sorted array', ask: 'Find a target in a sorted array that has been rotated at an unknown pivot.', d: 'Medium', pat: ['binary-index'], i: 'At each step one half is definitely sorted. Work out which, then decide whether the target lies inside it.' },
    { t: 'Word break', ask: 'Decide whether a string can be segmented into a sequence of dictionary words.', d: 'Medium', pat: ['memo'], i: 'dp[i] true if some j has dp[j] true and s[j:i] in the dictionary. It is O(n^2) substring work — say so.' },
    { t: 'Container with most water', ask: 'Given heights, pick two lines forming the container that holds the most water.', d: 'Medium', pat: ['two-pointers'], i: 'Move the SHORTER side inward. Being able to argue why moving the taller one cannot help IS the solution.' },
    { t: 'Number of provinces', ask: 'Given a connectivity matrix of cities, count the connected groups.', d: 'Medium', pat: ['union-find', 'graph-dfs'], i: 'Either works on a static graph. Union-Find wins the moment edges start arriving one at a time.' },
    { t: 'Reverse a linked list', ask: 'Reverse a singly linked list iteratively.', d: 'Easy', pat: [], str: ['linked-list'], i: 'prev, curr, and save next BEFORE reassigning. Type it until it is muscle memory — it hides inside harder problems.' },
    { t: 'Diameter of a binary tree', ask: 'Find the longest path between any two nodes, measured in edges.', d: 'Medium', pat: ['tree-dfs'], i: 'The recursion RETURNS height while UPDATING a global best. Returning one thing and recording another is itself a pattern.' },
    { t: 'Minimum window substring', ask: 'Find the shortest substring of s containing every character of t, including duplicates.', d: 'Hard', pat: ['window'], i: 'Track how many required characters are still missing. Shrink while valid, and record INSIDE the shrink — the opposite of the longest-window case.' },
    { t: 'Single number', ask: 'Every element appears twice except one. Find it in constant space.', d: 'Easy', pat: [], str: ['array'], tech: ['bit-tricks'], i: 'XOR the whole array. Pairs cancel to zero; the survivor is the answer.' },
    { t: 'Unique paths', ask: 'Count the paths from the top-left to the bottom-right of a grid, moving only right or down.', d: 'Medium', pat: ['memo'], str: ['grid'], i: 'dp[r][c] = dp[r−1][c] + dp[r][c−1]. Only the previous row is needed, so it collapses to O(n) space.' },
    { t: 'Trapping rain water', ask: 'Given an elevation map, compute how much water it traps after raining.', d: 'Hard', pat: ['two-pointers', 'deque-mono'], i: 'Water above a bar is min(maxLeft, maxRight) − height. Two pointers maintain both maxima with no extra arrays.' },
    { t: 'Clone a graph', ask: 'Return a deep copy of a connected undirected graph.', d: 'Medium', pat: ['graph-dfs'], i: 'A map original → copy is both the memo and the visited set. Create the copy BEFORE recursing or you loop forever.' },
    { t: 'Longest increasing subsequence', ask: 'Find the length of the longest strictly increasing subsequence.', d: 'Medium', pat: ['memo'], tech: ['lis-patience'], i: 'O(n^2) DP is the expected answer. The O(n log n) version keeps the smallest possible tail per length — know it exists.' },
    { t: 'House robber', ask: 'Maximise the sum you can take from an array without taking two adjacent elements.', d: 'Medium', pat: ['memo'], i: 'At each house: take it plus the best two back, or skip it. Two rolling variables suffice.' },
    { t: 'Non-overlapping intervals', ask: 'Find the minimum number of intervals to remove so that none overlap.', d: 'Medium', pat: ['greedy', 'sweep'], i: 'REVERSE THE QUESTION — maximise how many you keep. Sort by END time and greedily keep. The reversal is the insight.' },
    { t: 'Merge k sorted lists', ask: 'Merge k sorted linked lists into one sorted list.', d: 'Hard', pat: ['kway', 'heap-topk'], str: ['linked-list'], i: 'Heap holds one node per list. Push tuples carrying the list identity, since nodes are not comparable.' },
    { t: 'Find all anagrams in a string', ask: 'Return every start index where a permutation of p appears in s.', d: 'Medium', pat: ['window', 'hash-count'], i: 'The window never changes size: add one, remove one, compare count maps.' },
    { t: 'Serialize and deserialize a binary tree', ask: 'Encode a binary tree as a string and reconstruct it exactly.', d: 'Hard', pat: ['tree-dfs'], i: 'Preorder with an explicit null marker reconstructs uniquely. Without the markers it does not.' },
    { t: 'Happy number', ask: 'Repeatedly replace n by the sum of the squares of its digits. Does it reach 1?', d: 'Easy', pat: ['fast-slow'], i: 'Cycle detection on a sequence rather than a list. Recognising that is the whole point of the problem.' },
    { t: 'Largest rectangle in histogram', ask: 'Find the largest rectangle that fits under a histogram outline.', d: 'Hard', pat: ['deque-mono'], tech: ['sentinel'], i: 'Each bar is popped when a shorter one arrives, and the pop is where you compute its widest rectangle. Append a trailing zero to flush.' },
    { t: 'Word ladder', ask: 'Find the fewest one-letter transformations from a start word to an end word through a dictionary.', d: 'Hard', pat: ['tree-bfs'], i: 'Unweighted shortest path. Build neighbours by wildcarding each position, not by comparing all word pairs.' },
    { t: 'Accounts merge', ask: 'Merge account records that share any email address into single accounts.', d: 'Medium', pat: ['union-find'], i: 'Union by email, then group by root. The elegance is in choosing what the nodes are.' },
    { t: 'Decode ways', ask: 'Count the ways a digit string can be decoded into letters, where 1–26 map to A–Z.', d: 'Medium', pat: ['memo'], i: 'Climbing stairs, but each step is conditional on validity. Zeros are the entire difficulty.' },
    { t: 'Pacific Atlantic water flow', ask: 'Find the cells in a height grid from which water can reach both oceans.', d: 'Medium', pat: ['graph-dfs'], str: ['grid'], i: 'Do not search outward from every cell. Search INWARD from each ocean edge and intersect the reachable sets.' },
    { t: 'Min stack', ask: 'A stack supporting push, pop, top and getMin, all in constant time.', d: 'Medium', pat: [], str: ['stack'], i: 'Store the running minimum alongside each element. A single min variable fails on pop, which is the lesson.' },
    { t: 'Sliding window maximum', ask: 'Return the maximum of every window of size k as it slides along the array.', d: 'Hard', pat: ['deque-mono', 'window'], i: 'Deque of indices in decreasing value order. Front is the maximum; drop indices that fall out of the window.' },
    { t: 'Redundant connection', ask: 'In a graph built by adding edges to a tree, find the edge that creates a cycle.', d: 'Medium', pat: ['union-find'], i: 'Process edges in order; the first union whose endpoints already share a root is the answer.' },
    { t: 'Edit distance', ask: 'Find the minimum single-character insertions, deletions or substitutions to turn one string into another.', d: 'Hard', pat: ['memo'], i: 'Three moves into dp[i][j] from its three neighbours. Draw the grid before coding — the recurrence is visible in the picture.' },
    { t: 'Longest consecutive sequence', ask: 'Find the longest run of consecutive integers present in an unsorted array.', d: 'Medium', pat: ['hash-count'], i: 'Set membership, and only start counting from values with no predecessor. That guard is what makes it O(n).' },
    { t: 'Valid parentheses', ask: 'Decide whether a string of three bracket types is correctly nested and closed.', d: 'Easy', pat: [], str: ['stack'], i: 'Push openers, pop and match on closers, and require an empty stack at the end. That last check is half the answer.' },
    { t: 'Binary tree right side view', ask: 'Return the values visible from the right, one per level.', d: 'Medium', pat: ['tree-bfs'], i: 'Level order, keep the last node of each level. A DFS visiting right first also works, with a depth check.' },
    { t: 'Task scheduler', ask: 'Given tasks and a cooldown n between identical tasks, find the minimum total time.', d: 'Medium', pat: ['greedy', 'heap-topk'], i: 'The most frequent task fixes the skeleton and the rest fill the gaps. A formula beats a simulation here.' },
    { t: 'Alien dictionary', ask: 'Given words sorted in an unknown alphabet, recover the letter order.', d: 'Hard', pat: ['topo'], i: 'Each adjacent word pair gives exactly one constraint — the first position where they differ. Handle the invalid prefix case.' },
    { t: 'Subsets', ask: 'Generate every subset of a list of distinct integers.', d: 'Medium', pat: ['backtracking'], tech: ['bitmask-enum'], i: 'choose, recurse, un-choose — and append a COPY of the path. Bitmask enumeration is the alternative worth mentioning.' },
    { t: 'Find the median from a data stream', ask: 'Support adding numbers and querying the running median at any time.', d: 'Hard', pat: ['heap-topk'], i: 'Max-heap for the lower half, min-heap for the upper, sizes within one. The median sits at the tops.' },
    { t: 'Ship packages within D days', ask: 'Given package weights in order and D days, find the minimum ship capacity that suffices.', d: 'Medium', pat: ['binary-answer'], i: 'The same shape as Koko. Once you have seen two of these the family is instantly recognisable — which is why both are here.' },
    { t: 'Search suggestions system', ask: 'For each prefix of a search term, return up to three matching products in lexicographic order.', d: 'Medium', pat: ['trie'], i: 'The most front-end problem in the list. Storing the top three at each node answers each keystroke in O(1).' },
    { t: 'Longest palindromic substring', ask: 'Find the longest palindromic substring of a string.', d: 'Medium', pat: [], str: ['array'], tech: ['expand-centre'], i: 'There are 2n−1 centres, counting the gaps between characters. Even-length palindromes are where this is usually failed.' },
    { t: 'Network delay time', ask: 'Given a weighted directed network and a source, how long until every node receives a signal?', d: 'Medium', pat: ['dijkstra'], tech: ['lazy-deletion'], i: 'Dijkstra, then take the maximum of all final distances. Unreachable nodes mean the answer is −1 — check that before returning the max.' },
    { t: 'Maximum subarray', ask: 'Find the contiguous subarray with the largest sum.', d: 'Medium', pat: ['memo'], tech: ['kadane'], i: 'Kadane: at each step, extend the run or restart here. Initialise from the first element, not 0, or an all-negative array returns 0.' },
    { t: 'LRU cache', ask: 'Design a cache with O(1) get and put that evicts the least recently used key.', d: 'Medium', pat: [], str: ['hash', 'linked-list'], tech: ['lru-cache'], i: 'Hash map for lookup plus a doubly linked list for ordering. In Python OrderedDict does both; be ready to build it by hand.' },
    { t: 'Course schedule II', ask: 'Return a valid order in which all courses can be taken, or an empty result if impossible.', d: 'Medium', pat: ['topo'], i: 'Same machinery as Course schedule, but you emit the order. Placed far from it deliberately, so it has to be recognised cold.' },
  ],
};
